if command -v uv &> /dev/null; then
  if [ ! -d .venv ]; then
    uv venv
  fi
  export VIRTUAL_ENV="$PWD/.venv"
  PATH_add "$VIRTUAL_ENV/bin"
fi

# Project-specific environment variables
# Auto-activates with direnv
#export OPENAI_API_KEY="sk-..."
#export TERMUX_ENV="true"
